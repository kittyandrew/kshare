use flake

# Per-deployment values (Zitadel issuer, project audience, cli client id) live in .env (gitignored). Copy
# .env.example to .env and fill it in after `kshare` is registered in Zitadel; see docs/zitadel.md.
dotenv_if_exists .env
